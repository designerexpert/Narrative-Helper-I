Connect SoapSelection components to the Store, and set up a way to change the positive property on them from within each one of them.

Connect NarrativeBuilder to the store, and set up a way to listen for changes on the positive property of each SoapSelection item, in order to display the correct narrative text.